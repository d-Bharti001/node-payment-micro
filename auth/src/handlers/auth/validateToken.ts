import type { AuthServiceHandlers } from "proto/auth/AuthService";

export const validateToken: AuthServiceHandlers["ValidateToken"] = function (call, callback) {
    try {
        // TODO
    } catch (err) {
        // TODO
    }
};
